"""CLI entry point — minimal stub for future local anonymization."""


def main() -> None:
    print("Mithril Veil CLI — use the HTTP API or see README for development setup.")
    print("  uvicorn app.main:app --reload")


if __name__ == "__main__":
    main()
